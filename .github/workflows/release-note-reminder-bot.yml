name: Release Notes Reminder Bot

on:
  pull_request:
    types: [labeled]

jobs:
  release-note-reminder-bot:
    runs-on: ubuntu-latest
    name: Prompt for release notes
    steps:
      - name: Prompt for release notes
        uses: ponylang/release-notes-reminder-bot-action@forked-repos
        env:
           API_CREDENTIALS: "ponylang-main:c20e8f010d795dd3ab3c3e099e1d46766a3321bf"

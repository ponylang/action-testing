name: Tag testing

on:
  push:
    tags: release-*.*.*

jobs:
  github-ref:
    name: github ref
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: What's the ref?
        run: bash .ci-scripts/tags.bash

name: External event

on: repository_dispatch

jobs:
  hello-world:
    name: hello world
    runs-on: ubuntu-latest
    steps:
      - name: echo it
        run: echo "hello world!"
        if: github.event.event_type == "cloudsmith_event"

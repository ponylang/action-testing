name: External event

on: repository_dispatch

jobs:
  hello-world:
    name: hello world
    runs-on: ubuntu-latest
    steps:
      - name: echo it
        run: echo "$EVENT_TYPE"
        env:
          EVENT_TYPE: ${{ toJson(github.event) }}
